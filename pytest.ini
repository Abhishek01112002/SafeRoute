# pytest.ini
[pytest]
# Scan these directories for tests
testpaths = backend/tests mobile/test

# Add backend to PYTHONPATH so 'app' can be imported
pythonpath = backend

# Ignore specific files that cause collection errors
norecursedirs = .git .venv venv build dist

# Options
addopts = --strict-markers -ra
