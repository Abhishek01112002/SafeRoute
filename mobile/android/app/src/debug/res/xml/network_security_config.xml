<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds can call a local HTTP backend on emulator or physical device LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
