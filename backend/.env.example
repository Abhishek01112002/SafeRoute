// backend/.env.example
// Copy to .env and fill in values before running the server.

# REQUIRED — generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,http://10.0.2.2:8000

# Database path (relative to backend/)
DATABASE_URL=./saferoute.db

# SOS Push Notifications (Firebase Admin SDK)
# Download from Firebase Console > Project Settings > Service Accounts
FIREBASE_CREDENTIALS_PATH=./firebase-adminsdk.json

# SOS SMS Fallback
# Options: fast2sms | twilio
SMS_PROVIDER=fast2sms
SMS_API_KEY=

# Twilio (only if SMS_PROVIDER=twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Fast2SMS (only if SMS_PROVIDER=fast2sms)
FAST2SMS_SENDER_ID=SFRTST
